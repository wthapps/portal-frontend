import { RouterConfig } from '@angular/router';

import { SupportComponent } from './index';

export const SupportRoutes: RouterConfig = [
  {
    path: 'support',
    component: SupportComponent
  }
];
