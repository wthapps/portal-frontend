import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'my-subscription',
  templateUrl: 'subscription.component.html'
})

export class MySubscriptionComponent {
  pageTitle: string = 'Current subscription';

}
